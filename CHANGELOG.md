# Changelog

## 0.0.1 — 2026-09-17

首个版本（纯前端插件，无后端进程）。

**入口与面板**

- 对话窗口右上角注册 📎 按钮（官方扩展点 `chat.rightHeader.add`），点击开合面板
- 面板 fixed 停在对话栏左侧，不遮挡输入框；窗口缩放自适应，宽度写入 localStorage 记忆

**文件树与预览**

- 工作区文件树：懒加载目录，根可在「项目目录 / 工作区」间切换（复用 `/api/workspace/tree`）
- Monaco 只读预览（CDN AMD 加载 + zh-cn 语言包），按扩展名自动切换 20+ 语言高亮
- 只预览文本文件：按扩展名白/黑名单 + 内容嗅探（NUL 字节 / 大量替换字符）判定；
  二进制文件不发起文件请求、不塞进 Monaco，改为明确提示
- 左栏（文件树）宽度固定为 px：面板拉宽只加宽右侧预览；左栏与预览之间可拖拽分隔条
  （双击复位，宽度记忆）

**选区 → 送到对话**

- 选中代码后点「➤ 送到对话」或 Monaco 内 `Ctrl+K`：生成
  `关于 <路径>:<起>-<止>：` + 带语言标记的代码块，注入当前对话输入框并把光标移到末尾
- 注入策略三档降级：textarea 原生 setter + `input` 事件 → contenteditable `execCommand`
  → 剪贴板兜底，均带明确提示；注入后**回读校验**，被宿主回滚时当场发现并自动降级（附原因）
- 单次发送上限 300 行 / 30000 字符，超出截断并标注

**面板几何**

- 8 个拖拽手柄（四边 + 四角），最小 320×180、最大不超视口；手柄有 hover 高亮与对应光标
- 拖动 / 缩放改为直接写 DOM + rAF 节流，松手才提交状态
- 标题栏可拖动移动（越界自动钳制、位置写入 localStorage、拖动时锁定高度不塌缩）；
  手动定位后标题栏显示「· 已手动定位」并提供 **⌖ 复位** 按钮（回到"自动贴靠输入框上方"）
- 自动贴靠状态下拖左边缘只改宽度、仍保持自动跟随；拖其它边/角或标题栏则切换为手动几何

**文件树右键菜单**（文件与文件夹都支持）

| 菜单项 | 行为 |
|---|---|
| 插入路径到对话 | 在输入框**光标处**插入 `projects/xxx/index.js `（非追加末尾），自动补前后空格 |
| @ 引用到对话 | 插入 `@ projects/xxx/index.js `，与宿主自带「在聊天中引用」格式一致（新构建渲染为文件引用 chip） |
| 打开预览 | 仅文本文件可用；二进制时置灰并说明原因 |
| 复制路径 | 复制相对路径到剪贴板 |

- 菜单渲染在面板**外层**（面板 `overflow:hidden`，贴屏幕边缘时内层会被裁掉）
- 点别处 / 滚轮 / Esc / 再次右键都会关闭；贴边自动收进视口内

**错误处理（三档降级 + 可重试）**

- 输入框处于禁用/生成中、找不到对话输入框、空文件、目录条目超 500 条：均给明确提示，不静默
- 文件树 / 文件读取失败：显示原因 + 内联**重试**按钮；区分网络失败与 HTTP 错误、非 JSON 响应
- Monaco 加载加 20s 超时保护 + **重试**按钮
- toast 定时器改为可取消，避免新旧提示互相干扰

**已知边界**

- 依赖宿主对话窗口的 DOM（输入框、右键引用格式），宿主结构变化时可能需要跟进适配
- Monaco 走 CDN，离线环境不可用（会提示并可重试）
